import RegisterForm from "@/components/register/RegisterForm";

export const metadata = {
  title: "Reserve Your Easel — The Cozy Canvas",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
